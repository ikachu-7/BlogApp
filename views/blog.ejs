<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('./partials/head') %>
    <title>SeperateBlog</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
            margin: 0;
            padding: 0;
        }
        
        .container {
            max-width: 800px;
            margin: 40px auto;
            background-color: #ffffff;
            box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
            padding: 20px;
            border-radius: 4px;
        }
        
        h1 {
            font-size: 28px;
            margin-top: 0;
        }
        
        img {
            position: relative;
            max-width: 100%;
            height: auto;
            margin: 20px 0;
            border-radius: 4px;
            box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
        }
        
        p {
            font-size: 16px;
            line-height: 1.6;
        }
    </style>
</head>
<body>
    <%- include('./partials/navbar') %>
    <div class="container">
        <h1><%= blog?.title %></h1>
        <img src="<%= blog?.coverImageUrl %>" alt="Blog Cover Image">
        <pre><%= blog?.body %></pre>
        <h4>Author: <%= blog.createdBy.fullName %></h4>
    </div>
    <%- include('./partials/script') %>
</body>
</html>
